# Dependencies
node_modules/
src/node_modules/

# Build output
dist/
build/
src/dist/
src/build/

# BMAD install — users install this themselves via `npx bmad-method install`.
# This regenerates both _bmad/ (module source) and .claude/skills/ (agent
# skills). Both are install products, so neither is committed; see README setup.
_bmad/
.claude/skills/

# Ralph loop run artifacts
scripts/logs/

# Triage ledger — local, per-run measurement data (issue #2 Idea 4); not committed
.ralph/

# Environment
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
*.swp
*.swo
.vscode/
.idea/

# Claude Code session state
.claude/local/
