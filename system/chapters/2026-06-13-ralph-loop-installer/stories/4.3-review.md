REVIEW_PASSED

# Story 4.3: Release preparation — Code Review

**Reviewed:** 2026-06-13
**Verdict:** PASS — all acceptance criteria met, all DoD gates green.

## Scope reviewed

Story 4.3 deliverables (all four files from the implementation summary):

- `installer/package.json` — finalized npm metadata
- `installer/README.md` — npm.org display README (created)
- `installer/LICENSE` — MIT license copied into package scope (created)
- `PUBLISHING.md` — manual publish checklist at repo root (created)

This is System Track work; Node.js is permitted inside `installer/`. No Demo Track
artifacts (`docs/`, `src/`) were touched, and `scripts/ralph-loop.sh` loop semantics
were not modified.

## Acceptance criteria

- **AC1 — Package name resolved:** `npm view @seevali/ralph-loop` returns 404 Not Found
  (name available). `package.json:2` sets `"name": "@seevali/ralph-loop"`, matching the
  scoped-name preference in the spec's decision logic. ✓
- **AC2 — Publication-ready metadata:** `installer/package.json` contains `name`, `version`
  (0.1.0), `description`, `keywords`, `author` (with email), `license` (MIT), `repository`
  (with `directory: "installer"`), `homepage`, `bugs`, `funding`, `engines.node`, `bin.ralph`,
  and a restrictive `files` array. All present, no placeholders. ✓
- **AC3 — `npm pack` ≤ 1 MB unpacked:** `npm pack --dry-run` succeeds: 41 files,
  **349.3 kB unpacked** (well under the 1 MB budget). Tarball includes `LICENSE` (1.1 kB),
  `README.md`, `bin/ralph.js`, all `src/` modules, and `templates/loop/`. ✓
- **AC4 — `PUBLISHING.md` checklist:** Exists at repo root (167 lines) with Prerequisites,
  Pre-publish checklist (tests, doctor smoke test, pack size, git state), Publish workflow
  (login → publish → verify → cold-start), Rollback (<24 h unpublish and ≥24 h
  deprecate+bump), and a metadata appendix. Self-contained and copy-paste actionable. ✓
- **AC5 — No `npm publish` executed:** `npm view @seevali/ralph-loop@0.1.0` returns 404;
  package is not live. Nothing in the changes invokes publish. ✓

## Build / test gates

- Checkpoint command (`bash -n scripts/ralph-loop.sh && bash -n system/ralph-loop-system.sh
  && scripts/ralph-loop.sh --dry-run-prompts`): **PASS**
- `cd installer && npm test`: **227 tests, 0 fail, exit 0** — confirmed stable across 3
  consecutive runs.
- `installer/scripts/sync-templates.sh --check`: exit 0 (templates in sync).

## Non-blocking observations (not defects; no action required)

- `src/*.test.js` files are bundled in the tarball because the `files` array whitelists
  `src/`. This is exactly what the spec's `files` array prescribes, and unpacked size is
  still 349 kB, so it is not a concern. A future story could narrow this if footprint ever
  matters.
- `funding.url` points to `github.com/sponsors/seevali`; the spec marked this optional
  ("include only if you have a Sponsors page"). Harmless if the page does not yet exist.
- `engines.node` is `">=20.0.0"` vs the spec's `">=20"` — semantically identical.
- On the very first `npm test` invocation I observed a one-off run reporting 219 tests /
  exit 1 (the E2E suite did not register, likely a transient `/tmp` race or a SIGPIPE from
  piping into `tail`). It did not reproduce across three subsequent clean runs (227/0/exit 0
  each), so it is not treated as a flaky-test blocker. Worth a passing note only.

No cross-story root-cause issues found.
