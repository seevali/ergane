REVIEW_PASSED

# Story 3.1 — Non-interactive mode — Code Review

Reviewed the on-disk implementation against the story spec and acceptance
criteria. This is a System Track story; Node.js is permitted inside `installer/`
per the chapter's Story 1.1 exception. All changes are confined to `installer/`.

## Files reviewed

- `installer/bin/ralph.js` — added `--yes`, `--force`, `--list-options`, and the
  six per-question flags to the `install` subcommand; wires `parseCliArgs` /
  `validateCliArgs` / `listOptions` and passes `useDefaults` + `cliAnswers` to the
  wizard and `yes`/`force` to the writer.
- `installer/src/cli-parser.js` (new) — `FLAG_DEFINITIONS`, `parseCliArgs`,
  `validateCliArgs`, `listOptions`.
- `installer/src/colors.js` (new) — `isColorEnabled` (NO_COLOR presence + TTY),
  `colorize`, ASCII `note`.
- `installer/src/wizard.js` — `buildNonInteractivePlan` for the `--yes` path;
  plain `log()` output (no ANSI); interactive path unchanged except plan-summary
  color is now gated on `isColorEnabled`.
- `installer/src/writer.js` — `confirmConflicts` now branches on `yes`/`force`:
  force+yes overwrites installer-owned / skips user-owned; yes-without-force
  prints conflicts and exits non-zero; interactive unchanged.
- `installer/src/cli-parser.test.js`, `installer/src/colors.test.js`,
  `installer/src/wizard.test.js`, `installer/test/noninteractive.test.js` — unit +
  E2E coverage.

## Acceptance criteria — all verified end-to-end via the real CLI

1. **`--yes --force` into empty dir** → `install` exits 0; `doctor` on the result
   exits 0. PASS.
2. **Plain output in pipes** → install output redirected to a file contains zero
   ANSI escape sequences. PASS.
3. **Conflict detection** → second install without `--force` exits 1 immediately,
   no prompt. PASS.
4. **NO_COLOR respected** → `NO_COLOR=1` produces zero color codes;
   `isColorEnabled` returns false whenever `NO_COLOR` is defined, independent of
   TTY (picocolors/@clack also honor NO_COLOR natively for the interactive path).
   PASS.
5. **Per-question flags** → `--list-options` exits 0 and lists all six
   per-question flags plus the global flags; each defined wizard question
   (app-dir, checkpoint-command, stack-description, use-bmad, task-source,
   skip-npm-script) has a flag that pre-fills and skips its prompt. PASS.
6. **Non-TTY plain output** → the non-interactive path uses plain `console.log`
   with no spinners and no color, independent of `NO_COLOR`. PASS.

## Gates

- Checkpoint command (`bash -n scripts/ralph-loop.sh && bash -n
  system/ralph-loop-system.sh && ./scripts/ralph-loop.sh --dry-run-prompts`) →
  PASS.
- `cd installer && npm test` → 160 tests, 160 pass, 0 fail.
- No stack-rule violations: no new forbidden dependencies, Node confined to
  `installer/`, no imports reach outside the installer package, no `docs/`/`src/`
  changes, no edits to loop-semantic sections of `scripts/ralph-loop.sh`.
- Error/exit-code handling is consistent: 0 success, 1 conflict/validation
  error, 2 environment preflight failure.

## Notes (non-blocking)

- The loop-knob questions (`loopRetries`, `maxTokensPerTurn`) and the
  gitignore/npm-script confirmations have no dedicated per-question flag; under
  `--yes` they fall back to defaults. This matches the story's explicit
  enumeration of six flags and AC-5's "e.g." wording, so it is in scope, not a
  gap.

No blocking issues found.
