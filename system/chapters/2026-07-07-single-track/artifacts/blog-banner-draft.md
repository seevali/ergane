# DRAFT — NOT PUBLISHED

**What this is:** a short banner to prepend to the existing seevali.dev post *"Watching the Loop Forge Itself"* (published 2026-05-25, narrating the two-track `ralph-loop-demo` era). Publishing means editing the post in the `sites/seevali.dev` repo — the owner's action, not this chapter's. The old post stays intact below the banner as a record.

---

> **Update (2026-07-08).** Since this post was written, the repo has been renamed **Ergane** and gone single-track: the Demo Track described below (the Exchange Rates Dashboard under `src/`) was retired, and that same "watch it build an app" experience now ships through the installer's worked example instead. The two-track story here still holds as a record of how the loop got here. → *[follow-up post: "The loop leaves the nest"]* `<!-- TODO: link once the sequel is published -->`
